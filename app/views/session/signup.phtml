<h1 class="headers-provisorios">SessionController - signUpAction</h1>

<?php $this->flashSession->output(); ?>

<ul class="breadcrumbs">
	<li><a href="<?php echo $this->url->get(""); ?>">Home</a></li>
	<li><a href="<?php echo $this->url->get("sign-in-up"); ?>">Sign In/ Sign Up</a></li>
	<li class="current"><a href="#">Sign Up</a></li>
</ul>

<div class="row text-center">
		<div class="small-8 small-centered columns">
		<h2>Sign Up for Polaroid Map App</h2>

	<form data-abide action="<?php echo $this->url->get("sign-up");
	?>" method="post">
		<fieldset>
			<div class="name-field">
				<label for="fullname">Full Name: <small>required</small>
					<input type="text"
					       id="fullname"
					       placeholder="full name"
					       required pattern="alpha" name="user_full_name">
				</label>
				<small class="error">Full Name is required and must be a string.</small>
			</div>
			<div class="username-field">
				<label for="username">Username: <small>required</small>
					<input type="text"
					       id="username"
					       placeholder="username"
					       required pattern="alpha_numeric"
					       name="user_name">
				</label>
					<small class="error">User Name is required and must be a
					                     string.</small>
			</div>
			<div class="email-field">
				<label for="email">Email Adress: <small>required</small>
					<input type="email"
					       id="email"
					       placeholder="email"
					       required pattern="email"
					       name="user_email">
				</label>
					<small class="error">Email is required.</small>
			</div>

			<div class="password-field">
					<label for="password">Password (mininum 8 characters):
						<small>required</small>
					<input type="password"
					       placeholder="password"
					       id="password"
					       aria-invalid="false"
					       required pattern="alpha_numeric"
					       min="8"
					       maxlength="30"
					       size="30"
					       name="user_password">
				</label>
				<small class="error">Your password must match the
				                     requirements.</small>
			</div>

			<div class="password-confirmation-field">
				<label for="confirmpassword">Repeat Password:
					<small>required</small>
					<input type="password"
					       id="confirmpassword"
					       placeholder="repeat password"
					       required pattern="alpha_numeric"
					       min="8"
					       maxlength="30"
					       data-invalid=""
					       ata-equalto="user_password"
					       size="30"
					       name="user_password_confirmation">
				</label>
				<small class="error">Your password must match the above one
				                     .</small>
			</div>

			<input type="hidden"
			       name="<?php echo $this->security->getTokenKey() ?>"
			       value="<?php echo $this->security->getToken() ?>">

			<?php echo $this->tag->submitButton(array("Sign Up", "class" => "button small success")); ?>
		</fieldset>
	</form>
	</div>
</div>

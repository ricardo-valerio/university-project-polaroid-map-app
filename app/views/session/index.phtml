<h1 class="headers-provisorios">SessionController - indexAction</h1>

<?php $this->flashSession->output(); ?>

<ul class="breadcrumbs">
	<li><a href="<?php echo $this->url->get(""); ?>">Home</a></li>
	<li class="current"><a href="#">Sign In | SignUp</a></li>
</ul>

<div class="row">
	<div class="small-6 columns">
		<h2>Login</h2>
	<?php if ($this->request->hasQuery("return_to")): ?>
		<?php echo $this->tag->form(array('/session/login?return_to=' .
		                                  $this->request->getQuery("return_to"), "method" => "post")); ?>
	<?php else: ?>
		<?php echo $this->tag->form(array('/session/login' , "method" => "post")); ?>
	<?php endif ?>
		<fieldset>
				<label>Email/Username:
					<input type="text"
					       placeholder="email or username"
						   name="user_email_or_username">
				</label>
				<label>Password:
					<input type="password"
					       placeholder="password"
					       size="30"
					       name="user_password">
				</label>

				<input type="hidden"
				       name="<?php echo $this->security->getTokenKey() ?>"
				       value="<?php echo $this->security->getToken() ?>">
				<div class="text-center">
					<?php echo $this->tag->submitButton(array("Sign In", "class" => "button small")); ?>
				</div>
			</fieldset>
		<?php echo $this->tag->endForm(); ?>
	</div>
	<div class="small-6 columns">
		<h2>Don't have an account yet?</h2>
		<p>Create an account offers the following advantages:</p>
		<ul>
			<li>Advantage A</li>
			<li>Advantage B</li>
			<li>Advantage C</li>
		</ul>
		<div class="text-center">
			<?php echo $this->tag->linkTo(array(
							'/sign-up',
							'Sign Up!',
							'class' => 'button small success'
						));
			?>
		</div>
	</div>
</div>

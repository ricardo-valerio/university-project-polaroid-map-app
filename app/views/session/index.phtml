<?php $this->flashSession->output(); ?>

<div class="row collapse">
	<div class="small-12 large-8 small-centered columns">
		<h2 class="text-center">Sign In</h2>
	<?php if ($this->request->hasQuery("return_to")): ?>
		<?php echo $this->tag->form(array('/session/login?return_to=' .
		                                  $this->request->getQuery("return_to"), "method" => "post")); ?>
	<?php else: ?>
		<?php echo $this->tag->form(array('/session/login' , "method" => "post")); ?>
	<?php endif ?>
		<fieldset>
				<label>Email/Username:
					<input type="text"
					       placeholder="email or username"
						   name="user_email_or_username">
				</label>
				<label>Password:
					<input type="password"
					       placeholder="password"
					       size="30"
					       name="user_password">
				</label>

				<input type="hidden"
				       name="<?php echo $this->security->getTokenKey() ?>"
				       value="<?php echo $this->security->getToken() ?>">
				<div class="text-center">
					<?php echo $this->tag->submitButton(array(
						"Sign In",
						"class" => "button expand success"
					)); ?>
				</div>

				<div class="text-center">
					<?php echo $this->tag->linkTo(array(
						'/sign-up',
						'Sign Up!',
						'class' => 'button expand secondary'
					));
					?>
				</div>
			</fieldset>
		<?php echo $this->tag->endForm(); ?>
</div>
